{
  "name": "devops-demo",
  "version": "1.0.0",
  "main": "server.js",
  "dependencies": {
    "express": "^4.19.2"
  },
  "scripts": {
    "build": "mkdir -p dist && cp server.js dist/server.js && echo Build complete"
  }
}
